export declare const scaleToContainMatch: (targetWidth: number, targetHeight: number, compareWidth: number, compareHeight: number, options: {
    widthLimit: number;
    heightLimit: number;
}) => number;
export declare const scaleToCoverMatch: (targetWidth: number, targetHeight: number, compareWidth: number, compareHeight: number, options: {
    widthLimit: number;
    heightLimit: number;
}) => number;
